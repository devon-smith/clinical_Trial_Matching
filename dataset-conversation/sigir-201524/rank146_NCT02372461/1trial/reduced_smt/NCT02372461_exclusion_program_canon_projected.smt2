;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert (not patient_has_finding_of_edema_of_foot_now))
(assert (not patient_has_finding_of_inpatient_stay_inthepast2weeks))
(assert (not patient_has_diagnosis_of_asthma_now))
(assert (not patient_has_diagnosis_of_tuberculosis_now))
(assert (not patient_has_undergone_antibiotic_therapy_inthepast48hours))
(assert (not patient_has_finding_of_bulging_fontanelle_now))
(assert (not patient_has_diagnosis_of_congenital_heart_disease_now))
