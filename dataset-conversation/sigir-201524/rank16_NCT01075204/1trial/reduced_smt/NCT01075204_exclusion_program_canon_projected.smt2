;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert (not patient_has_hypersensitivity_to_macrolide_inthehistory))
(assert (not patient_has_intolerance_to_macrolide_inthehistory))
(assert (not patient_is_exposed_to_cytochrome_p450_enzyme_now))
(assert (not patient_is_taking_colchicine_containing_product_now))
(assert (not patient_is_taking_digoxin_containing_product_now))
(assert (not patient_has_finding_of_chronic_disease_now))
