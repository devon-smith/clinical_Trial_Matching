;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert (not patient_has_finding_of_cardiogenic_shock_now))
(assert (not patient_has_taken_low_molecular_weight_heparin_containing_product_inthepast8hours))
(assert (not patient_is_exposed_to_anticoagulant_now))
(assert (not patient_has_finding_of_delirium_now))
(assert (not patient_has_finding_of_dementia_now))
