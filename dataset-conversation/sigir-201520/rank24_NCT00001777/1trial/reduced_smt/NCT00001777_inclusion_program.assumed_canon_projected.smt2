;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert patient_has_finding_of_behavior_finding_now)
(assert patient_has_finding_of_frontal_lobe_syndrome_now)
(assert (not patient_has_contraindication_to_sertraline_now))
(assert (not patient_is_pregnant_now))
(assert (not patient_is_breastfeeding_now))
