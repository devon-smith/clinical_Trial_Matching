;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert (not patient_has_diagnosis_of_secondary_malignant_neoplastic_disease_now))
(assert (not patient_has_finding_of_multiple_injuries_now))
(assert (not patient_has_finding_of_pathological_fracture_now))
(assert (not patient_has_finding_of_aphasia_now))
