;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert (not patient_has_finding_of_illiteracy_now))
(assert (not patient_has_finding_of_severe_hearing_loss_now))
(assert (not patient_has_finding_of_severe_visual_impairment_now))
(assert (not patient_has_finding_of_impaired_cognition_now))
