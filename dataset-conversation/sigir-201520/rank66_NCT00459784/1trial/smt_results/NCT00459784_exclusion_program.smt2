;; ===================== Declarations for the exclusion criterion (REQ 0) =====================
;; No variables to declare, as the requirement is "none" (no exclusion criteria).

;; ===================== Constraint Assertions (REQ 0) =====================
;; No exclusion criteria to assert; always eligible.
(assert
(! true
:named REQ0_COMPONENT0_OTHER_REQUIREMENTS)) ;; "A patient is excluded if none."
