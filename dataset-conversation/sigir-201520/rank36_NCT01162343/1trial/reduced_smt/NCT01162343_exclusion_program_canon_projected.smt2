;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert (not patient_has_finding_of_dementia_now))
(assert (not patient_has_finding_of_severe_intellectual_disability_now))
(assert (not patient_has_finding_of_aphasia_now))
(assert (not patient_has_finding_of_barrier_to_communication_now))
(assert (not patient_has_diagnosis_of_coma_now))
