;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert (not patient_has_finding_of_high_suicide_risk_now))
(assert (not patient_has_finding_of_suicidal_intent_now))
(assert (not patient_has_finding_of_acute_disease_now))
(assert (not patient_has_finding_of_aphasia_now))
