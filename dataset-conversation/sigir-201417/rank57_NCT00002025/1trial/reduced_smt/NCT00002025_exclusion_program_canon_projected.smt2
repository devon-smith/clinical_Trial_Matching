;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert (not patient_has_finding_of_allergy_to_acyclovir_now))
(assert (not patient_has_finding_of_allergy_to_ganciclovir_now))
(assert (not patient_is_taking_alkylating_agent_containing_product_now))
(assert (not patient_is_taking_cytokine_containing_product_now))
(assert (not patient_is_taking_foscarnet_containing_product_now))
(assert (not patient_is_taking_interferon_containing_product_now))
