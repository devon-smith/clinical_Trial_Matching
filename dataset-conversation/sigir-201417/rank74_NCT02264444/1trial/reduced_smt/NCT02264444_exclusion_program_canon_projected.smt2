;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert (not patient_is_pregnant_now))
(assert (not patient_is_breastfeeding_now))
(assert (not patient_has_finding_of_malignant_hyperthermia_inthehistory))
(assert (not patient_has_contraindication_to_neostigmine_now))
(assert (not patient_has_finding_of_continuous_opioid_dependence_now))
(assert (not patient_has_diagnosis_of_long_qt_syndrome_now))
