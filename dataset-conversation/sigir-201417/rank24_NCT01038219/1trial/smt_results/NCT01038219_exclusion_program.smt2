;; ===================== Declarations for the exclusion criterion (REQ 0) =====================
;; No variables to declare, as no exclusion criteria are specified.

;; ===================== Constraint Assertions (REQ 0) =====================
;; No exclusion criteria: always eligible.
(assert
(! true
:named REQ0_COMPONENT0_OTHER_REQUIREMENTS)) ;; "No patient is excluded, because no exclusion criteria are specified."
