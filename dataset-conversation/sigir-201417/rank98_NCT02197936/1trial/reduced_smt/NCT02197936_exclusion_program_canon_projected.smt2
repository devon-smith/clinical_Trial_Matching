;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert (not patient_has_childbearing_potential_now))
(assert (not patient_is_pregnant_now))
(assert (not patient_has_diagnosis_of_malignant_tumor_of_female_genital_organ_now))
(assert (not patient_has_diagnosis_of_endometriosis_now))
(assert (not patient_has_finding_of_uterine_leiomyoma_now))
