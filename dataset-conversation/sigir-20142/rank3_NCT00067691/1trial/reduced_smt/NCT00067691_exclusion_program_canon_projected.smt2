;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert patient_has_finding_of_deformity_of_chest_wall_now)
(assert (not patient_has_undergone_acupuncture_inthehistory))
(assert (not patient_has_diagnosis_of_myoneural_disorder_now))
(assert (not patient_has_finding_of_anemia_now))
(assert (not patient_has_finding_of_uncontrolled_pain_now))
(assert (not patient_has_finding_of_heart_valve_disorder_now))
