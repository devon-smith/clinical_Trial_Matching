;; ===================== Declarations for the exclusion criterion (REQ 0) =====================
;; No exclusion criteria specified; no variables to declare.

;; ===================== Constraint Assertions (REQ 0) =====================
;; No exclusion criteria specified; always eligible.
(assert
(! true
    :named REQ0_COMPONENT0_OTHER_REQUIREMENTS)) ;; "The patient is NOT excluded by any exclusion criterion, as no exclusion criterion is specified."
