;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Additional OR-clauses (SAT-filtered)
(assert (or patient_is_exposed_to_microbial_agent_now patient_has_diagnosis_of_legionella_infection_now))
(assert (or patient_is_exposed_to_microbial_agent_now patients_legionella_ag_is_positive_now))
