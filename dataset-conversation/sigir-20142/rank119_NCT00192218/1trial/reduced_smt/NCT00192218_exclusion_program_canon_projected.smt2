;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert (not patient_has_finding_of_immunosuppression_now))
(assert (not patient_is_pregnant_now))
(assert (not patient_has_diagnosis_of_guillain_barr_syndrome_inthehistory))
(assert (not patient_has_finding_of_fever_inthepast72hours))
