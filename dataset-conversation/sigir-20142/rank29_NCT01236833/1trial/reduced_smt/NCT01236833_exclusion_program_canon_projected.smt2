;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert (not patient_has_finding_of_small_for_gestational_age_fetus_now))
(assert (not patient_has_finding_of_pneumatosis_cystoides_intestinalis_now))
(assert (not patient_has_finding_of_pneumoperitoneum_now))
(assert (not patient_has_finding_of_bilious_vomiting_now))
(assert (not patient_has_finding_of_bowel_sounds_absent_now))
(assert (not patient_has_finding_of_intestinal_obstruction_now))
(assert (not patient_has_finding_of_hypernatremia_now))
