;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert (not patient_has_undergone_bronchoscopy_inthepast14days))
(assert (not patient_has_finding_of_physical_activity_target_strenuous_exercise_inthepast24hours))
(assert (not patient_has_finding_of_systemic_disease_now))
