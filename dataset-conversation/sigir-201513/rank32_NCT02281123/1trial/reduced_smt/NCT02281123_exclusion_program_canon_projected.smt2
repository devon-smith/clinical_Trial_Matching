;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert (not patient_has_finding_of_joint_pain_now))
(assert (not patient_has_diagnosis_of_arthritis_inthehistory))
(assert (not patient_is_exposed_to_non_steroidal_anti_inflammatory_agent_inthehistory))
