;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert (not patient_has_finding_of_allergy_to_penicillin_now))
(assert (not patient_has_allergy_to_beta_lactam_antibacterial_now))
(assert (not patient_has_finding_of_illness_now))
(assert (not patient_has_diagnosis_of_seizure_disorder_now))
(assert (not patient_is_pregnant_now))
(assert (not patient_is_breastfeeding_now))
(assert (not patient_has_finding_of_alcohol_abuse_now))
(assert (not patient_is_taking_probenecid_containing_product_now))
