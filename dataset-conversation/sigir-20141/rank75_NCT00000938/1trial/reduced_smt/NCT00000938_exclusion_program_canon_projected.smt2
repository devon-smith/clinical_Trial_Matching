;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert (not patient_is_pregnant_now))
(assert (not patient_is_lactating_now))
(assert (not patient_has_finding_of_allergy_to_ceftriaxone_now))
(assert (not patient_has_finding_of_allergy_to_doxycycline_now))
(assert (not patient_can_undergo_medication_administration_intravenous_now))
