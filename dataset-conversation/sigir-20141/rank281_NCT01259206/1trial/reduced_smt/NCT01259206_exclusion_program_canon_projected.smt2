;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert (not patient_has_finding_of_injury_of_foot_inthehistory))
(assert (not patient_has_finding_of_vascular_disorder_now))
(assert (not patient_has_finding_of_malignant_neoplastic_disease_now))
(assert (not patient_has_finding_of_lymphedema_now))
