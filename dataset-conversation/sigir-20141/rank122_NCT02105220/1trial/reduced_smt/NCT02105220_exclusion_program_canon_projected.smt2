;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert (not patient_has_finding_of_esophageal_varices_now))
(assert (not patient_has_finding_of_pneumothorax_now))
(assert (not patient_is_pregnant_now))
(assert (not patient_has_undergone_oxygenation_index_measurement_now_outcome_is_abnormal))
