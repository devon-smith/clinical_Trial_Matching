;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert (not patient_sex_is_female_now))
(assert (not patient_has_finding_of_inflammatory_disorder_now))
(assert (not patient_has_finding_of_neoplastic_disease_now))
(assert (not patient_has_diagnosis_of_protein_s_deficiency_disease_now))
(assert (not patient_is_undergoing_low_molecular_weight_heparin_therapy_now))
