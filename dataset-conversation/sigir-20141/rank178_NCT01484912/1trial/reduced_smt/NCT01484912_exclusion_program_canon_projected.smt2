;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert (not patient_has_finding_of_abnormal_cardiac_conduction_now))
(assert (not patient_has_finding_of_myocardial_infarction_inthepast3months))
(assert (not patient_has_finding_of_preinfarction_syndrome_now))
(assert (not patient_has_finding_of_rhythm_from_artificial_pacing_now))
(assert (not patient_has_finding_of_ventricular_pre_excitation_now))
(assert (not patient_has_finding_of_hepatic_failure_now))
(assert (not patient_has_finding_of_renal_failure_syndrome_now))
