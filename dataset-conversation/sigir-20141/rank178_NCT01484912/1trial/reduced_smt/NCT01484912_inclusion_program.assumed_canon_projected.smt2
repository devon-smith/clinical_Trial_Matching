;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert patient_has_finding_of_exercise_tolerance_test_abnormal_inthepast7days)
(assert patient_has_undergone_exercise_tolerance_test_inthepast7days)
(assert patient_has_finding_of_exercise_tolerance_test_abnormal_now)
