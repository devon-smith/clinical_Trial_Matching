;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert patient_can_undergo_implantation_of_insertable_loop_recorder_now)
(assert (not patient_has_finding_of_fever_now))
(assert (not patient_has_finding_of_persistent_infection_now))
(assert (not patient_has_finding_of_sepsis_now))
(assert (not patient_has_finding_of_electrocardiogram_abnormal_now))
(assert (not patient_has_finding_of_thyrotoxicosis_now))
