;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert (not patient_has_finding_of_chronic_infectious_disease_now))
(assert (not patient_has_finding_of_diabetes_mellitus_now))
(assert (not patient_has_finding_of_disorder_of_gastrointestinal_tract_now))
(assert (not patient_has_finding_of_hyperlipidemia_now))
(assert (not patient_has_finding_of_hypertensive_disorder_now))
(assert (not patient_has_finding_of_tobacco_smoking_behavior_finding_now))
(assert (not patient_is_taking_drug_or_medicament_now))
(assert (not patient_is_taking_nutritional_supplement_now))
