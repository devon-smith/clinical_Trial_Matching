;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert (not patient_has_finding_of_right_ventricular_failure_now))
(assert (not patient_has_finding_of_ventricular_arrhythmia_inthehistory))
(assert (not patient_has_contraindication_to_sildenafil_now))
(assert (not patient_is_pregnant_now))
