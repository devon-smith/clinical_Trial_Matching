;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert (not patient_has_finding_of_alcohol_intake_above_recommended_sensible_limits_now))
(assert (not patient_is_pregnant_now))
(assert (not patient_is_lactating_now))
(assert (not patient_has_finding_of_chronic_disease_now))
(assert (not patient_has_finding_of_dyslipidemia_now))
(assert (not patient_has_finding_of_hyperglycemia_now))
(assert (not patient_is_taking_vitamin_supplement_now))
