;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert patients_blood_pressure_is_abnormal_now)
(assert patient_has_diagnosis_of_ischemic_stroke_now)
(assert patient_has_finding_of_poor_hypertension_control_now)
