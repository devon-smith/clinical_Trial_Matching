;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert patient_has_finding_of_dissatisfaction_with_body_image_now)
(assert (not patient_has_diagnosis_of_anorexia_nervosa_now))
(assert (not patient_has_diagnosis_of_binge_eating_disorder_now))
(assert (not patient_has_diagnosis_of_bulimia_nervosa_now))
(assert (not patient_has_finding_of_body_weight_now))
(assert (not patient_has_finding_of_normal_weight_now))
