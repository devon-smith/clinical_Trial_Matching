;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert patient_has_finding_of_ischemic_chest_pain_now)
(assert patient_has_finding_of_myocardial_infarction_now)
(assert patient_has_finding_of_st_segment_elevation_now)
(assert patient_has_finding_of_culprit_lesion_of_coronary_artery_now)
