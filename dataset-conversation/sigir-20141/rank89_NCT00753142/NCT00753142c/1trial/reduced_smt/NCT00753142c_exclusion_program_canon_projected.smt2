;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert (not patients_anti_pancreatic_islet_cell_antibody_is_positive_now))
(assert (not patients_glutamic_acid_decarboxylase_antibody_is_positive_now))
(assert (not patient_has_finding_of_blood_coagulation_disorder_now))
(assert (not patient_has_finding_of_coag_bleeding_tests_abnormal_now))
(assert (not patient_has_finding_of_thrombocytopenic_disorder_now))
(assert (not patient_is_pregnant_now))
