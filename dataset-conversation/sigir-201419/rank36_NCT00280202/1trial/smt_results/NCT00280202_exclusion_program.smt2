;; ===================== Declarations for the exclusion criterion (REQ 0) =====================
;; No variables to declare, as there are no exclusion criteria specified.

;; ===================== Constraint Assertions (REQ 0) =====================
;; No exclusion criteria specified; always eligible.
(assert
(! true
:named REQ0_COMPONENT0_OTHER_REQUIREMENTS)) ;; "There are no exclusion criteria specified; no patient is excluded based on exclusion criteria."
