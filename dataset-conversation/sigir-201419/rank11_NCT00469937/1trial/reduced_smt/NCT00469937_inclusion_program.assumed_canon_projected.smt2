;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert patient_has_finding_of_secondary_malignant_neoplasm_of_brain_now)
(assert (not patient_can_undergo_stereotactic_radiosurgery_now))
(assert (not patient_has_finding_of_secondary_malignant_neoplasm_of_brain_stem_now))
