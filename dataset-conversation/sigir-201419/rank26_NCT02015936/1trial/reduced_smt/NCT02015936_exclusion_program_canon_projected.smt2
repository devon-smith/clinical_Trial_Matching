;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert (not patient_has_finding_of_patient_in_hospital_now))
(assert (not patient_has_finding_of_secondary_malignant_neoplasm_of_bone_now))
(assert (not patient_has_finding_of_loss_of_consciousness_inthehistory))
(assert (not patient_has_finding_of_seizure_inthehistory))
(assert (not patient_has_diagnosis_of_impaired_cognition_now))
