;; ===================== PROJECTED CANON (QE) =====================
;; Each clause below is entailed by Proj = ∃W.F over the canonical vocabulary.
;; Units appear first for clarity; OR-clauses (SAT-filtered) follow.
;; ==================================================================

;; Unit Bool constraints (entailed)
(assert (not patient_has_diagnosis_of_asthma_inthehistory))
(assert (not patient_has_symptoms_of_asthma_now))
(assert (not patient_has_finding_of_eosinophil_count_raised_now))
(assert (not patient_has_finding_of_asthma_now))
(assert (not patient_has_finding_of_unable_to_communicate_now))
(assert (not patient_has_finding_of_exercise_physically_impossible_now))
(assert (not patient_has_undergone_lung_volume_reduction_surgery_inthehistory))
(assert (not patient_has_undergone_transplant_of_lung_inthehistory))
(assert (not patient_has_finding_of_alcohol_abuse_now))
(assert (not patient_has_finding_of_finding_relating_to_drug_misuse_behavior_now))
(assert (not patient_has_finding_of_solvent_misuse_now))
